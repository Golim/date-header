# `Date` Header

Check the behavior of different web caches and CDNs regarding the `Date` HTTP header. When serving cached content, do they preserve the original `Date` header from the origin server, or do they replace it with the time the content was served from the cache?
